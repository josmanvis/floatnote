# Quick Task 002: Publish a New Build

## Task
Publish a new release build of Floatnote to GitHub Releases and npm.

## Steps

1. Commit unstaged changes (freeze feature + drag handle styling fix)
2. Push all commits to origin (fix auth scope for workflow files)
3. Bump version with `npm version patch` (1.0.2 → 1.0.3)
4. Push tag `v1.0.3` to trigger release workflow
