# Quick Task 004: Publish a New Build

## Task
Publish v1.0.6 release with shape drawing feature to GitHub Releases and npm.

## Steps

1. Push shape drawing commits to origin (rebase over remote changes)
2. Bump version with `npm version patch` (1.0.5 → 1.0.6)
3. Push tag `v1.0.6` to trigger release workflow
4. Verify release and npm publish succeed
